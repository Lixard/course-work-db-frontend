import {SexEnumPipePipe} from './sex-enum-pipe.pipe';

describe('SexEnumPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SexEnumPipePipe();
    expect(pipe).toBeTruthy();
  });
});
